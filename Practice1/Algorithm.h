#include <string>
using namespace std;

class Algorithm
{
public:
    static void replaceSpaces(string& input);
    
    static int CompressedLength(const string& input);
    
    static void CompressString(string& input);
};